// Adds two command line arguments (numbers).
 console.log(Number(process.argv[2]) + Number(process.argv[3]));

// Si no le ponemos el Number antes del process.argv, se concatenarán los argumentos en las posiciones 2 y 3
